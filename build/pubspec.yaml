name: build_blog
description: Builds my blog.
publish_to: none

executables:
  build_blog: build

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  markdown: ^2.1.5
  charcode: ^1.1.3
  cli_util: ^0.2.0
  args: ^1.6.0
  file: ^5.2.0
  googleapis: ^0.55.0
  googleapis_auth: ^0.2.12
  html: ^0.14.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  xml: ^4.2.0
